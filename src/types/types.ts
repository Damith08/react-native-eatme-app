export type RegisterFormData = {
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  password: string;
  address: string;
  contact: string;
};

export type LoginFormData = {
  email: string;
  password: string;
};
