export type RegisterFormData = {
  firstName: string;
  surname: string;
  email: string;
  password: string;
  address: string;
  mobile: string;
};

export type LoginFormData = {
  email: string;
  password: string;
};
